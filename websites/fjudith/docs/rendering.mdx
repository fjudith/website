---
sidebar_position: 5
title: Diagrams & Slides
---

import LikeC4 from '@site/src/components/LikeC4';
import Slides from '@site/src/components/Slides';
import WebsiteArchitecture from '@site/src/calm/website.mdx';

# Diagrams & Slides

This site supports three rendering integrations. Each has its own source format
and build step, wired into `npm start` and `npm run build`.

## LikeC4

Architecture-as-a-model with interactive diagrams. Edit the model in
`likec4/model.c4`; run `npm run likec4:codegen` to regenerate the TypeScript
model (this also runs automatically on build).

<LikeC4 viewId="index" height={480} />

A second view of the same model:

<LikeC4 viewId="website" height={420} />

## CALM

FINOS [Common Architecture Language Model](https://calm.finos.org). Edit the
architecture in `calm/website.architecture.json`; `npm run calm:build`
validates it and renders it to a Mermaid diagram embedded below.

<WebsiteArchitecture />

## Slidev

Developer-focused Markdown slide decks. Author in `slides/intro.md`;
`npm run slidev:build` compiles the deck to a static app served at
`/slides/intro/` and embedded here.

<Slides name="intro" />

Open the deck [full screen](pathname:///slides/intro/).
